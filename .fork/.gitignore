detect-report.tsv

# rerere scratch: git writes thisimage* while a conflict is being resolved; only
# preimage/postimage are resolutions. Tracking them left the tree dirty after every run.
rr-cache/**/thisimage*

# Throwaway workspace for .fork/tests/test-rr-audit.sh
tests/t/
